<!--
CIRG recommended Sysmon configuration.

Focused on the ATT&CK techniques covered by the built-in detection rules in
backend/soc/rules_seed.py: credential access (LSASS), process creation with
command lines, network connections (C2 detection), and file/registry
persistence indicators. Deploy with:

    sysmon64.exe -accepteula -i sysmon_config.xml

or, to reconfigure an existing install:

    sysmon64.exe -c sysmon_config.xml
-->
<Sysmon schemaversion="4.90">
  <EventFiltering>

    <!-- Event ID 1: Process creation — captures full command lines -->
    <RuleGroup name="" groupRelation="or">
      <ProcessCreate onmatch="exclude">
        <Image condition="end with">svchost.exe</Image>
        <Image condition="end with">conhost.exe</Image>
      </ProcessCreate>
    </RuleGroup>

    <!-- Event ID 3: Network connection — needed for C2 / beaconing detection -->
    <RuleGroup name="" groupRelation="or">
      <NetworkConnect onmatch="exclude">
        <Image condition="end with">chrome.exe</Image>
        <Image condition="end with">msedge.exe</Image>
        <Image condition="end with">firefox.exe</Image>
        <DestinationPort condition="is">443</DestinationPort>
        <DestinationPort condition="is">80</DestinationPort>
      </NetworkConnect>
    </RuleGroup>

    <!-- Event ID 10: Process access — LSASS credential-dumping detection -->
    <RuleGroup name="" groupRelation="or">
      <ProcessAccess onmatch="include">
        <TargetImage condition="end with">lsass.exe</TargetImage>
      </ProcessAccess>
    </RuleGroup>

    <!-- Event ID 11: File creation in commonly-abused locations -->
    <RuleGroup name="" groupRelation="or">
      <FileCreate onmatch="include">
        <TargetFilename condition="contains">\Startup\</TargetFilename>
        <TargetFilename condition="end with">.ps1</TargetFilename>
        <TargetFilename condition="end with">.hta</TargetFilename>
      </FileCreate>
    </RuleGroup>

    <!-- Event ID 12/13/14: Registry persistence (Run keys, services) -->
    <RuleGroup name="" groupRelation="or">
      <RegistryEvent onmatch="include">
        <TargetObject condition="contains">\CurrentVersion\Run</TargetObject>
        <TargetObject condition="contains">\Services\</TargetObject>
      </RegistryEvent>
    </RuleGroup>

    <!-- Event ID 22: DNS query — supports DNS-exfiltration / C2 hunting -->
    <RuleGroup name="" groupRelation="or">
      <DnsQuery onmatch="exclude">
        <QueryName condition="end with">.microsoft.com</QueryName>
        <QueryName condition="end with">.windowsupdate.com</QueryName>
      </DnsQuery>
    </RuleGroup>

  </EventFiltering>
</Sysmon>
